#include <stdio.h>

int main()
{
    int age = 25;     // Integer variable
    float pi = 3.14;  // Floating point variable
    char grade = 'A'; // Character variable

    printf("Age: %d\n", age);
    printf("Pi: %.2f\n", pi);
    printf("Grade: %c\n", grade);
    return 0;
}
