#include <stdio.h>

int main()
{
    // For loop
    for (int i = 0; i < 5; i++)
    {
        printf("For Loop: %d\n", i);
    }

    // While loop
    int j = 0;
    while (j < 5)
    {
        printf("While Loop: %d\n", j);
        j++;
    }

    return 0;
}
